# Core libraries for data science and machine learning
# Pinned: the serialized models (.pkl) are unpickled with these exact versions.
# Leaving scikit-learn/joblib unpinned lets the deploy host pull an incompatible
# release, which breaks model loading (e.g. "No module named '_loss'").
numpy==2.4.0
pandas==2.3.3
scikit-learn==1.8.0
joblib==1.5.3
scipy==1.16.3
xgboost==3.2.0
lightgbm==4.6.0

# Library for bioinformatics
biopython

# AMP-specific feature calculation
modlAMP

# Visualization
matplotlib>=3.5.0
seaborn>=0.12.0

# Web server
flask>=3.0.0
gunicorn>=21.0.0
psycopg2-binary>=2.9.0
